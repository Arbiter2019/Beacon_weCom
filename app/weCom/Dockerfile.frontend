FROM node:22-alpine AS build
WORKDIR /srv/wecom/frontend
COPY frontend/package.json frontend/package-lock.json* ./
RUN npm install
COPY frontend ./
RUN npm run build

FROM nginx:1.27-alpine
COPY --from=build /srv/wecom/frontend/dist /usr/share/nginx/html
EXPOSE 80
